name: Polyglot SDK consumer matrix

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: '41 3 * * 2,5'
  workflow_dispatch:
  repository_dispatch:
    types: [clients-updated]

permissions:
  contents: read

jobs:
  sdk-clients:
    uses: zed-pkg-test/.github/.github/workflows/sdk-client-language-matrix.yml@df3eed96202810b7e7d6667fabc3041b19e6a22f
    with:
      clients_repository: scintilla-run/scintilla-clients
      clients_ref: main
      clients_root: clients
      interfaces_repository: scintilla-run/scintilla-interfaces
      interfaces_ref: main
      interfaces_path: scintilla-interfaces
      interfaces_dependency: scintilla-run/scintilla-interfaces
    secrets:
      read_token: ${{ secrets.TEST_ORG_READ_TOKEN }}
